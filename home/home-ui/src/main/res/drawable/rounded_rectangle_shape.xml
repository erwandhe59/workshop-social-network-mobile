<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/colorCardBackground" />
    <corners android:radius="16dp"/> <!-- Rayon des coins arrondis -->
    <stroke
        android:width="2dp"
        android:color="@color/colorPrimary" /> <!-- Bordure optionnelle -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- Padding interne pour espacer le contenu -->
</shape>